import { useEffect,useState } from 'react';import { ScrollView,StyleSheet,Text } from 'react-native';import { listLogs,loadServerUrl } from '@/src/services/api';
export default function Logs(){const [lines,setLines]=useState<string[]>([]);useEffect(()=>{void loadServerUrl().then(listLogs).then(x=>setLines(x?.lines??[])).catch(()=>setLines(['日志加载失败，请确认管理员权限']))},[]);return <ScrollView style={s.page}><Text style={s.title}>日志</Text>{lines.map((x,i)=><Text key={i} style={s.line}>{x}</Text>)}</ScrollView>}const s=StyleSheet.create({page:{flex:1,padding:16,backgroundColor:'#111827'},title:{fontSize:24,fontWeight:'800',color:'#fff',marginBottom:15},line:{fontFamily:'Courier',fontSize:11,lineHeight:17,color:'#d1d5db'}});
