import { useEffect,useState } from 'react';import { ScrollView,StyleSheet,Text,View } from 'react-native';import { loadServerUrl,monitorStats } from '@/src/services/api';
export default function Monitor(){const [data,setData]=useState<any>({});useEffect(()=>{void loadServerUrl().then(monitorStats).then(setData).catch(()=>setData({error:'需要管理员权限'}))},[]);return <ScrollView style={s.page}><Text style={s.title}>监控</Text>{Object.entries(data).map(([k,v])=><View style={s.row} key={k}><Text style={s.key}>{k}</Text><Text style={s.value}>{typeof v==='object'?JSON.stringify(v):String(v)}</Text></View>)}</ScrollView>}const s=StyleSheet.create({page:{flex:1,padding:20,backgroundColor:'#f5f7fb'},title:{fontSize:28,fontWeight:'800',color:'#172033',marginBottom:14},row:{backgroundColor:'#fff',padding:14,borderRadius:8,marginTop:10},key:{fontWeight:'700',color:'#344054'},value:{color:'#657187',marginTop:6}});
