import { useCallback, useEffect, useState } from 'react';
import { Alert, Pressable, RefreshControl, ScrollView, StyleSheet, Text, View } from 'react-native';
import { listFiles, listHistory, listSubscriptions, loadServerUrl } from '@/src/services/api';
type Mode='history'|'files'|'subscriptions';
export default function LibraryScreen(){const [mode,setMode]=useState<Mode>('history');const [items,setItems]=useState<any[]>([]);const [loading,setLoading]=useState(false);const load=useCallback(async()=>{setLoading(true);try{const s=await loadServerUrl();const d=mode==='history'?await listHistory(s):mode==='files'?await listFiles(s):await listSubscriptions(s);setItems(Array.isArray(d)?d:(d?.items??d?.history??d?.files??d?.subscriptions??[]));}catch{Alert.alert('加载失败','无法读取服务端内容。')}finally{setLoading(false)}},[mode]);useEffect(()=>{void load()},[load]);return <ScrollView style={styles.page} refreshControl={<RefreshControl refreshing={loading} onRefresh={()=>{void load()}}/>}><Text style={styles.title}>内容库</Text><View style={styles.tabs}>{([['history','历史'],['files','文件'],['subscriptions','订阅']] as const).map(([k,t])=><Pressable key={k} onPress={()=>setMode(k)} style={[styles.tab,mode===k&&styles.tabOn]}><Text style={[styles.tabText,mode===k&&styles.tabTextOn]}>{t}</Text></Pressable>)}</View>{items.length===0?<Text style={styles.empty}>{loading?'加载中...':'暂无内容'}</Text>:items.map((x,i)=><View key={String(x.id??x.history_id??i)} style={styles.card}><Text style={styles.cardTitle} numberOfLines={2}>{x.title??x.name??x.url??'未命名内容'}</Text><Text style={styles.meta} numberOfLines={2}>{x.status??x.platform??x.created_at??x.path??''}</Text></View>)}</ScrollView>}
const styles=StyleSheet.create({page:{flex:1,padding:24,backgroundColor:'#f7f9fc'},title:{fontSize:28,fontWeight:'800',color:'#172033'},tabs:{flexDirection:'row',marginTop:20,backgroundColor:'#e9edf4',borderRadius:8,padding:3},tab:{flex:1,height:38,alignItems:'center',justifyContent:'center',borderRadius:6},tabOn:{backgroundColor:'#fff'},tabText:{color:'#657187',fontWeight:'700'},tabTextOn:{color:'#172033'},empty:{textAlign:'center',marginTop:100,color:'#657187'},card:{backgroundColor:'#fff',borderWidth:1,borderColor:'#e5eaf2',borderRadius:8,padding:15,marginTop:12},cardTitle:{fontSize:16,fontWeight:'700',color:'#172033'},meta:{fontSize:13,color:'#657187',marginTop:7}});
